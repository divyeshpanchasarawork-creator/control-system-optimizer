package org.divyesh.panchasara.control_system_optimizer.model;

/**
 * Enumeration of the physical systems the framework can instantiate.
 * New systems register here and provide a factory in the system registry.
 */
public enum SystemType {
	SPRING_DAMPER
}